<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- Back view should be first to be drawn first! -->
    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/header_cover"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        fresco:actualImageScaleType="fitXY" />

    <LinearLayout
        android:id="@+id/relation_container_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"

        android:layout_marginRight="10dip"
        android:layout_marginTop="17dip"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/relation_text_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="follow"
            android:textColor="@color/WhiteColor"
            android:textSize="14dp" />

        <ImageView
            android:id="@+id/relation_image_view_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add" />
    </LinearLayout>

    <TextView
        android:id="@+id/brand_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="20dip"
        android:layout_marginTop="15dip"
        android:text="Hello"
        android:textColor="@color/WhiteColor"
        android:textSize="@dimen/ic_album_overflow_height"
        android:textStyle="bold" />
</RelativeLayout>